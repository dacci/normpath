# Path normalizer

Normalizes path components to NFC.

## Usage

```console
Path normalizer

Usage: normpath <PATH>...

Arguments:
  <PATH>...  Path to the root directory to process

Options:
  -h, --help     Print help
  -V, --version  Print version
```
